"""Cross-cutting personas used by all three pipelines.

Each persona file declares a PERSONA dict with structured metadata
(full_name, role_title, years_experience, firm_pedigree, specialties,
prompt_template). The dashboard reads these dicts directly.
"""
